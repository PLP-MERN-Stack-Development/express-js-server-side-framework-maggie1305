# Express Products API

A RESTful API built using Express.js implementing CRUD operations, middleware, and error handling.

## 🚀 How to Run

1. Clone the repository
2. Install dependencies:
   ```bash
   npm install
